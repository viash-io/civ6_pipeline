  docker.enabled = true
  def viash_temp = System.getenv("VIASH_TEMP") ?: "/tmp/"
  docker.runOptions = "-i -v ${baseDir}:${baseDir} -v $viash_temp:$viash_temp"
  process.container = "dataintuitive/viash"
  params {
    convert_plot__input = "viash_no_value"
    convert_plot__output = "viash_no_value"
    id = ""
    testScript = ""
    testResources = [  ]
    convert_plot {
      name = "convert_plot"
      container = "civ6_save_renderer_convert_plot"
      containerTag = "main_build"
      containerRegistry = "ghcr.io"
      containerOrganization = "viash-io/civ6_pipeline"
      command = "convert_plot"
      tests {
        isDefined = false
        testScript = "NA"
        testResources = [  ]
      }
      arguments {
        input {
          name = "input"
          otype = "--"
          required = true
          type = "file"
          direction = "Input"
          multiple = false
          multiple_sep = ":"
          value = "${params.convert_plot__input}"
          example = "input.pdf"
          description = "A PDF input file."
        }
        output {
          name = "output"
          otype = "--"
          required = true
          type = "file"
          direction = "Output"
          multiple = false
          multiple_sep = ":"
          value = "${params.convert_plot__output}"
          example = "output.png"
          description = "Output path."
        }
      }
    }
  }